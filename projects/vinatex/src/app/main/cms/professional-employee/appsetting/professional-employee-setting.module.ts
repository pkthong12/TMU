import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';
import { ProfessionalEmployeeSettingRoutes } from './professional-employee-setting.routing';

@NgModule({
  imports: [RouterModule.forChild(ProfessionalEmployeeSettingRoutes)],
  declarations: [
  ],
})

export class ProfessionalEmployeeSettingModule {}