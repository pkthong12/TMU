import { MapImgToServerPipe } from './map-img-to-server.pipe';

describe('MapImgToServerPipe', () => {
  it('create an instance', () => {
    const pipe = new MapImgToServerPipe();
    expect(pipe).toBeTruthy();
  });
});
